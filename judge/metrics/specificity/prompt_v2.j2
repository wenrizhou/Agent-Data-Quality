以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估用户 query 和 conversation 的“具体性”。
“具体性”指的是：用户 query 和 conversation 是否清楚表达了用户想让 assistant 执行的任务动作，以及该任务作用的核心对象、实体、参数或目标是否明确。

query 和 conversation 可能包含一个或多个请求。请判断其中是否存在以下问题：

1. 意图不清
指 query 和 conversation 对具体操作或任务动作表达模糊，导致 assistant 难以判断用户到底想做什么或调用什么工具。

2. 意图缺失
指整个 query 都没有说明任何具体任务，且无法从 conversation 上下文或工具中确定操作。assistant 无法判断用户希望执行什么操作，也无法合理选择工具。

3. 对象不清
指 query 和 conversation 已经表达了任务动作，也提供了某种目标对象或自然语言描述，但对象、实体、范围或执行所需关键信息不够明确，assistant 仍需要用户补充才能稳定执行。

4. 对象缺失
指 query 和所有 conversation 都没有说明具体对象，且无法从 conversation 上下文中确定对象。assistant 无法判断用户希望操作哪个对象、实体、地点、账号、文件、订单、事件、商品、人员或其他目标。

注意：如果调用工具本身不需要参数，或任务天然不需要具体对象，则不要因为对象缺失而扣分。

评分标准：

- 2 分：具体 / 已澄清
  query 和 conversation 明确说明了用户想执行的任务动作，并且给出了完成任务所需的核心对象、实体、参数或目标。assistant 可以判断要做什么、对什么做，以及大致如何执行。
  如果只缺少非核心细节、可选偏好、输出风格、排序方式、数量上限等，不影响 assistant 理解任务动作和核心对象，也可以判为 2 分。

- 1 分：部分具体
  query 和 conversation 中的任务动作基本明确，且可以识别出自然语言层面的目标对象，但缺少执行任务所需的核心参数、精确标识符或必要上下文，assistant 需要追问才能稳定执行。
  例如：用户说明了要查某个酒店、角色、艺人、账号、订单、事件、文章、邮箱关联资料或附近地点，但没有提供工具所需的 hotel_id、character_id、Spotify URL、user_id、ticket_id、article_url、email、坐标或位置。

- 0 分：不具体
  query 和所有 conversation 对话信息都缺少具体任务意图，或完全缺少可识别的核心对象，导致 assistant 无法判断用户想做什么或对什么做，也无法通过上下文合理确定下一步应该追问什么对象。

具体判定规则：

1. 自然语言对象清楚但缺少工具执行参数，通常判为 1 分，而不是 0 分。
   例如用户说“查询 Beyoncé 的所有专辑”，但没有提供 Spotify URL；用户说“查看 Hôtel Le Marais 的支付政策”，但没有提供 hotel_id；用户说“查看 VILLAIN789 的角色详情”，但没有提供工具所需 is_id。这些情况说明目标对象在自然语言层面是清楚的，但执行参数不足，应判为“对象不清”而不是“对象缺失”。

2. 只有当核心对象完全不可识别时，才判为 0 分。
   例如用户只说“帮我查一下这个”“处理那个账号”“获取这个用户的信息”，且 conversation 中没有任何可唯一确定的对象；或者多个可能对象并存而用户没有说明要操作哪一个。

3. 工具 schema 中的 default value 不能自动代表用户真实对象。
   如果用户没有提供 user_id、URL、email、坐标、ticket_id、hotel_id 等核心对象，而工具文档里存在默认值，不要仅因为工具可以用默认值调用就判为 2 分。默认值只能说明工具可能可调用，不能证明用户对象已经明确。此类情况通常判为 1 分；如果连自然语言对象也没有，则判为 0 分。

4. 可追问补全不等于完全具体。
   如果 assistant 知道应该追问什么，例如“请提供邮箱地址”“请提供 hotel_id”“请提供坐标”，说明任务意图和对象方向部分明确，通常判为 1 分。只有当已给信息足以直接稳定执行或缺失信息不影响核心任务时，才判为 2 分。

5. 应基于完整 conversation 判断，而不是只看最后一句 query。
   如果前文、system 信息、tool response 或 assistant/user 后续对话已经唯一确定对象，则不算对象缺失。如果对象只能从 assistant 自行假设的 tool call 参数中得出，而用户和上下文并未支持，则不能把该假设视为用户对象已经明确。

6. 地点、平台、环境上下文缺失时，根据其是否是核心对象判断。
   “around me”“near me”“当前账号”“这个平台”等表达，如果 conversation 或 metadata 中没有可确定的位置、账号或平台，且该信息是任务执行的核心条件，通常判为 1 分；如果任务可以提供通用建议而不依赖精确环境，可以判为 2 分。

7. 多子任务需要分别判断。
   如果部分子任务动作和对象明确，但另一个核心子任务缺少对象或关键上下文，通常整体判为 1 分。只有所有核心子任务都缺少意图或对象时，才判为 0 分。

注意事项：
1. 只评估具体性，不评价 query 和 conversation 是否安全、真实等无关标准。
2. 不要因为 query 和 conversation 简短就扣分；只要动作和对象明确，就可以判为 2 分。
3. 不要因为工具最终可能执行失败就直接判低分；只有当失败反映出用户请求本身意图不清或对象不清时，才影响具体性。
4. 如果工具不需要参数，或任务不依赖具体对象，不应因为对象缺失而判低分。
5. 如果 query 和 conversation 包含多个子任务，需要分别检查每个子任务的动作和对象是否明确。
6. 如果用户使用“这个”“它”“上面那个”等指代表达，但上下文中可以唯一确定所指对象，则不算对象缺失。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
