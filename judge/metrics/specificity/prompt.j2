以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估用户 query 和 conversation 的“具体性”。
“具体性”指的是：用户 query 和 conversation 是否清楚表达了用户想让 assistant 执行的任务动作，以及该任务作用的核心对象、实体、参数或目标是否明确。

query 和 conversation 可能包含一个或多个请求。请判断其中是否存在以下问题：

1. 意图不清
指 query 和 conversation 对具体操作或任务动作表达模糊，导致 assistant 难以判断用户到底想做什么或调用什么工具。
2. 意图缺失
指整个 query 都没有说明任何具体任务，且无法从 conversation 上下文或工具中确定操作。assistant 无法判断用户希望执行什么操作，也无法合理选择工具。
3. 对象不清
指 query 和 conversation 对任务作用的核心对象、实体、目标或必要参数表达模糊，导致 assistant 难以执行任务或向工具传递参数。
4. 对象缺失
指所有 query 和 conversation 都没有说明具体对象，且无法从 conversation 上下文或工具中确定对象。assistant 无法判断用户希望执行操作的对象，也无法给调用工具传递参数。
注意：如果调用工具本身不需要参数，或任务天然不需要具体对象，则不要因为对象缺失而扣分。

评分标准：

- 2 分：具体 / 已澄清
  query 和 conversation 明确说明了用户想执行的任务动作，并且给出了完成任务所需的核心对象、实体、参数或目标；或者这些信息可以通过上下文、工具功能或向用户追问补全。assistant 可以直接判断要做什么、对什么做，以及大致如何执行。
- 1 分：部分具体
  query 和 conversation 中存在意图不清或对象不清，或只有部分任务、操作具体，assistant 无法完全理解用户的方向。
- 0 分：不具体
  query 和所有 conversation 对话信息都缺少具体任务意图，或缺少所有关键对象信息，导致 assistant 无法判断用户想做什么或对什么做，也无法合理选择工具。

注意事项：
1. 只评估具体性，不评价 query 和 conversation 是否安全、真实等无关标准。
2. 不要因为 query 和 conversation 简短就扣分；只要动作和对象明确，就可以判为 2 分。
7. 如果工具不需要参数，或任务不依赖具体对象，不应因为对象缺失而判低分。
8. 如果 query 和 conversation 包含多个子任务，需要分别检查每个子任务的动作和对象是否明确。
9. 如果用户使用“这个”“它”“上面那个”等指代表达，但上下文中可以唯一确定所指对象，则不算对象缺失。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
