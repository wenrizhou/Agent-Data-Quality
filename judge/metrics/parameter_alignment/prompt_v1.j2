以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估工具调用轨迹中的“参数对齐”质量。
“参数对齐”指的是：assistant 在已经发起的工具/API 调用中，所填写的字段名称、字段值和字段来源是否与用户 query、conversation、工具文档以及前序 tool response 保持一致。

请只评估“已调用工具/API 的字段填写是否正确”这一件事。不要评价这个工具/API 是否应该被调用，也不要因为工具本身看起来不是最佳选择而直接扣分。

请判断工具调用字段中是否存在以下问题：

1. 参数值不合法
指字段名称、字段类型、字段格式、枚举取值、上下限、必填/可选约束等违反工具文档要求，或字段值明显不符合工具文档描述的业务含义。

2. 用户参数提取错误
指 assistant 从用户 query 和 conversation 中错误提取、错误推断、遗漏或篡改字段值，导致字段值与用户真实意图不一致。

3. 轨迹参数提取错误
指 assistant 在多步工具调用中，错误提取、错误继承或编造了前序 tool response 中的 ID、URL、名称、坐标、日期、候选项、文件路径或其他中间结果。

参数类型判定：

1. 结构化槽位字段
包括 ID、URL、email、账号、日期、时间、地点、坐标、货币、数量、region、enum、文件路径、产品编号、订单号等。结构化槽位字段必须能从用户 query、conversation、工具文档默认值或前序 tool response 中合理追溯；如果无法追溯且 assistant 填入了具体值，通常属于参数提取错误。

2. 开放生成字段
包括 search query、prompt、message、summary、description、body、title、reason、response_strategy、issue_summary 等自然语言字段。开放生成字段允许 assistant 根据用户目标进行合理改写、总结、扩展或组织语言；只有当它引入了与用户需求或前序结果冲突的具体事实、遗漏核心约束，或把缺失的结构化事实伪装成确定内容时，才判为参数问题。

3. 系统或环境字段
包括 api_key、session、token、origin、referer、sender、app_id、redirect_uri 等运行环境相关字段。除非用户、conversation、工具文档默认值或前序 tool response 明确提供，否则不要把随意填写的具体值视为可靠来源。若这类字段是可选且不影响核心任务，可以考虑轻微问题；若是核心必填字段且随意编造，通常属于严重问题。

评分标准：

- 2 分：参数对齐正确。
  所有核心字段的名称、值和来源均与用户 query、conversation、工具文档和前序 tool response 一致；开放生成字段合理表达用户目标；没有明显非法字段、错误提取或编造字段。
- 1 分：轻微参数问题。
  存在非核心字段错误、可选字段不严谨、轻微格式问题、开放生成字段轻微遗漏、字段值可通过上下文修正，或前面调用有轻微字段问题但后续调用已修正；整体任务仍有合理执行可能。
- 0 分：参数对齐错误。
  存在核心结构化字段非法、核心字段凭空编造、关键约束遗漏/反转、前序 ID/URL/候选项继承错误，导致工具调用无法正确执行、查询对象明显错误，或明显偏离用户需求。

注意事项：
1. 只评估工具调用里的字段填写，不评价 query 和 conversation 是否真实、安全等无关标准。
2. 如果 conversation 中没有任何工具调用，则不存在参数对齐问题，应判为 2 分。
3. 如果用户 query 和 conversation 本身缺少必要字段，而 assistant 没有编造字段，而是留空、使用 #missing、使用工具文档允许的默认值或提出澄清，不应判为参数对齐错误。
4. 如果字段虽然与用户表达不完全一致，但属于合理标准化，例如 “tomorrow” 被转换为正确日期，“New York City” 被规范为 “NYC”，或自然语言搜索词被改写为等价检索 query，可以判为正确。
5. 如果输入中没有提供当前日期，不要仅因为 assistant 将 today、this week、this year、next Friday 等相对时间转换为具体日期就判错；只有当转换与已给定日期、metadata 或 conversation 明确冲突时，才判为错误。
6. 搜索 query 可以包含同义词、实体扩展、前序结果中的实体、合理的检索限定词；但不能凭空加入未由用户或前序结果支持的具体 ID、URL、账号、邮箱、日期、地点等结构化事实。
7. 如果后续调用依赖前序 tool response，应重点检查相关字段是否正确继承；不能把前序 response 中不存在的 ID、URL、候选项或具体数值当成已知事实。
8. 如果一个工具/API 看起来无关，但该调用内部字段本身合法且来源可追溯，不要仅因工具用途可疑而判为参数错误。
9. 如果存在多个工具调用，应逐个检查字段问题；只要核心工具调用中的核心字段存在严重错误，整体应判为 0。
10. 如果错误只出现在无关紧要的可选字段上，且不影响核心任务，可以考虑判为 1。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
