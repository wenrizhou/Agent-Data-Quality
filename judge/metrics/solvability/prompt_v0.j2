以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估用户 query 和 conversation 在给定工具/API 条件下的“可解性”。
“可解性”指的是：根据用户 query 和 conversation 中表达的需求，以及给定工具/API 的功能说明，判断这些工具是否有可能满足用户需求。

请判断 query 和 conversation，与 tools 之间是否存在以下问题：

1. 指向性不可解
指用户 query 和 conversation 明确指定了某个工具/API，但该工具/API 的功能无法满足用户需求。
2. 整体不可解
指给出的所有工具/API 都无法满足用户 query 和 conversation 的需求。

评分标准：

- 2 分：可解
  给定工具/API 中至少有一个工具，或多个工具组合后，能够满足用户 query 和 conversation 的核心需求。即使缺少部分参数，也可以通过追问或上下文补全，不影响可解性判断。
- 0 分：不可解
  用户指定的工具无法满足需求，或给定的所有工具/API 都无法满足用户需求。工具能力与用户核心需求明显不匹配。

注意事项：
1. 只评估“工具/API 的功能是否能够覆盖用户需求”的可解性，不评价 query 和 conversation 是否真实、安全等无关标准。
2. 如果 query 和 conversation 明确指定了某个工具/API，应优先判断该指定工具是否能满足需求；如果没有指定具体工具/API，则判断所有给定工具中是否至少存在一个工具或工具组合可以满足需求。
3. 如果 query 和 conversation 缺少必要参数或对象不明确，但工具能力方向正确，不要判为不可解。
4. 如果工具只能满足 query 和 conversation 的非核心需求，而核心需求无法满足，应判为 0 分。
5. 不要因为需要多步工具调用就判为不可解，只要工具组合能够完成需求即可。
6. 不要因为工具返回结果可能为空、失败或不完整就判为不可解；只要工具能力描述上支持该需求即可。
7. 只评估 query 和 conversation 与工具/API 功能之间的匹配关系，不评估后续调用参数是否正确，也不评估最终答案是否正确。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
