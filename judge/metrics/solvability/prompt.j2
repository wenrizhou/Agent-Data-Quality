以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估用户 query 和 conversation 在给定工具/API 条件下的“可解性”。
“可解性”指的是：根据用户 query 和 conversation 中表达的请求，以及给定工具/API 的功能说明，判断这些工具/API 是否具备处理该请求所需的能力。这里的“处理”包括但不限于：查询相关信息、判断请求是否可执行、执行用户请求的动作、确认当前状态、解释为什么无法完成用户原始意愿、或基于工具结果提供替代方案。

注意：可解性评估关注的是“工具/API 的能力是否覆盖该请求”，而不是用户的原始意愿在现实条件下是否一定能成功实现。

请判断 query 和 conversation，与 tools 之间是否存在以下问题：

1. 指向性不可解
指用户 query 和 conversation 明确指定了某个工具/API，但该工具/API 的功能无法支持处理该请求。
这里的“无法支持处理”包括：无法查询必要信息、无法判断请求是否可执行、无法执行必要动作、也无法提供拒绝/替代方案所需的依据。
2. 整体不可解
指给出的所有工具/API 组合都无法支持处理用户请求。也就是说，工具/API 既不能获取处理该请求所需的核心信息，也不能判断请求是否可执行，不能执行相关动作，也不能提供有依据的失败原因或替代方案。

评分标准：

* 2 分：可解
  给定工具/API 中至少有一个工具，或多个工具组合，具备处理用户请求所需的能力。
  包括以下情况：
  * 工具/API 可以直接完成用户请求；
  * 工具/API 可以获取必要信息，并支持 assistant 判断用户请求是否可执行；
  * 工具/API 可以发现用户原始意愿在现实条件下不可行，并支持 assistant 给出有依据的说明；
  * 工具/API 可以提供替代方案、下一步建议、相关政策或状态说明；
  * 缺少部分参数，但可以通过追问、上下文补全，或后续工具调用获取，不影响可解性判断。

* 0 分：不可解
  用户指定的工具，或给定的所有工具/API，都不具备处理用户请求所需的能力。
  即工具能力与用户请求明显不匹配，无法获取核心信息，无法判断请求是否可执行，无法执行相关动作，也无法提供有依据的失败说明或替代方案。

注意事项：

1. 只评估 query 和 conversation 与工具/API 功能之间的匹配关系，即工具/API 是否具备处理该请求所需的能力。不要评价 query 和 conversation 是否真实、安全、完整、合理，除非这些因素直接影响工具能力匹配判断。
2. 不要把“用户原始意愿是否最终成功实现”作为可解性判断标准。如果工具/API 能够判断用户原始意愿不可行，并支持 assistant 基于工具结果说明原因或提供替代方案，应判为 2 分。
3. 如果 query 和 conversation 明确指定了某个工具/API，应优先判断该指定工具是否具备处理该请求的能力；如果没有指定具体工具/API，则判断所有给定工具中是否至少存在一个工具或工具组合可以处理该请求。
4. 如果 query 和 conversation 缺少必要参数或对象不明确，但工具能力方向正确，且可以通过追问、上下文补全或工具查询获得，不要判为不可解。
5. 如果工具只能获取非核心信息，无法支持处理用户请求的核心目标，也无法判断请求是否可执行或提供有依据的失败说明，应判为 0 分。
6. 不要因为需要多步工具调用就判为不可解。只要工具组合整体具备处理该请求所需的能力，即应判为 2 分。
7. 不要因为工具返回结果可能为空、失败、不完整，或现实状态导致用户原始意愿无法达成，就判为不可解。只要工具能力描述上支持查询、判断、执行或解释该请求，即应判为 2 分。
8. 只评估工具/API 功能是否覆盖请求，不评估后续调用参数是否正确，不评估工具调用顺序是否正确，也不评估最终答案是否正确。
9. assistant 可以在有足够工具信息支持的情况下指出用户需求不可行、拒绝执行某个动作、说明失败原因，或给出替代方案。不要因为 assistant 没有顺应用户原始意愿就判为不可解。


输出要求：

* 只输出一个严格 JSON object。
* 不要输出 Markdown。
* 不要输出推理过程、分析过程或 JSON 之外的任何文本。
* JSON 必须符合以下 JSON Schema：
  {{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
