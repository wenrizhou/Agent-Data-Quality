以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估 assistant 最后一轮 response 是否充分满足 user message 和 conversation context 中提出的所有当前有效需求。

“需求满足度”（user_requirement_fulfillment）指的是：assistant 的最后一轮 response 是否让用户在当前轮目标上拿到了可用结果、明确确认、完成的操作、充分解释，或在无法完成时拿到了准确且可执行的替代/升级路径。

这里的“需求”包括：

1. 主任务或核心目标；
2. 明确提出的子问题、子步骤或多工具流程；
3. 用户指定的格式、语言、长度、结构、字段、单位、时间范围、地点范围、顺序、是否等待确认等约束；
4. 用户要求执行的操作及其结果确认；
5. 多轮上下文中仍然有效的用户目标、ID、选择、偏好和约束。

只评估“最后一轮 assistant response 对用户需求的满足程度”。不要评价其它指标本身，例如工具选择、参数对齐、最小化、真实性或安全性，除非这些问题直接导致用户需求没有被满足。

# 核心判定原则

先识别当前最后一轮用户真正想完成什么，再判断最后一轮 assistant response 是否交付了这个目标。

- 0 分：核心需求基本没有被满足，用户拿不到可用结果。
- 1 分：有正确方向、部分结果、合理第一步或有用说明，但仍有重要需求未完成。
- 2 分：当前用户需求在可用工具、政策和上下文下已经完整满足；或者无法完成时，assistant 给出了充分、准确、可执行的替代/升级路径。

评分时不要因为 response 很长、语气好、调用了工具、包含部分相关信息，就自动给高分。必须看用户是否真的拿到了当前任务需要的结果或下一步。

# 硬判定流程

请按以下顺序评分，不要跳步。

1. 识别当前有效需求
   结合 query、conversations、tools 和 metadata，列出最后一轮用户仍然需要 assistant 满足的主任务、子任务和显式约束。

2. 判断最后一轮 response 的交付状态
   检查 assistant 是否给出了用户可见的结果、确认、操作状态、最终结论、文件/链接/ID、计算值、分析、建议、模板、代码、替代方案或升级路径。

3. 区分“动作已发起”和“任务已完成”
   如果用户需要看到工具结果、数据、分析或确认，但 assistant 只停在 tool call，则通常只能给 1 分。只有当当前轮需求本身就是“发起/执行这个工具动作”，或工具调用就是该轮的最终必需动作时，才可以给 2 分。

4. 检查多子任务覆盖情况
   如果用户有多个核心子任务，必须分别判断每个子任务是否完成。一个重要子任务未完成，通常不能给 2 分。

5. 检查格式和约束
   如果格式、语言、单位、字段、时间范围、地理范围、顺序或输出结构是用户明确要求的一部分，遗漏会影响评分。若该约束是核心可用性条件，严重时可判 0；一般遗漏判 1。

6. 检查限制、拒绝和替代方案
   如果任务无法完成，判断 assistant 是否准确说明原因，并提供足够具体、可执行的替代步骤、外部资源、需要用户补充的信息，或正确升级/转人工。

7. 检查不可靠内容
   如果 response 中存在编造、未验证、占位符、虚构工具结果、虚构 ID/URL/时间/政策/事实等内容，判断它是否破坏核心需求。破坏核心答案时给 0；削弱但仍有部分价值时给 1。

8. 基于当前轮范围给最终分数
   评分重点是最后一轮用户需求。不要因为早先轮次有未完成的旧任务而自动扣分；但如果旧目标在当前轮仍然有效，或当前轮明确要求继续/完成旧目标，则必须纳入评分。

# 评分标准

## 2 分：充分满足

满足以下条件之一，且不存在重要遗漏或严重不可靠内容时，给 2 分：

1. assistant 完整回答了用户的主任务，并覆盖所有重要子需求。
   包括直接给出用户要的数据、列表、结论、计算值、比较、分析、推荐、说明、模板、代码或创作内容。

2. 正确使用工具并把结果清楚展示给用户。
   不是只调用工具，而是返回了用户需要的结果、状态、ID、链接、文件、确认、计算值或解释。

3. 多步骤任务完整闭环。
   例如 search -> detail -> verify -> recommend，lookup -> action -> confirmation，file read -> extract -> email，booking -> payment -> confirmation 等流程都完成，并在 final response 中汇总。

4. 操作已经完成并明确确认。
   例如预订、退款、创建 note/todo/poll、发送邮件、提交报告、发帖、保存文件、生成 QR/image/webhook/password、转人工等，assistant 给出了完成状态、确认号、链接、case ID、reference ID 或必要说明。

5. 所有显式格式和约束被满足。
   包括指定语言、JSON/HTML/代码/表格/列表、exact final phrase、boxed answer、字段集合、单位、时间范围、地点范围、数量限制、简洁程度、专业语气等。

6. 对事实题、计算题、选择题给出准确且足够的最终答案。
   包括 country code、电影/历史/词义答案、数学计算、单位换算、颜色转换、日期判断、multiple-choice 且遵守要求的结尾格式。

7. Advisory / planning / writing 类任务完整可用。
   对方案、计划、邮件、模板、路线、调试步骤、架构建议、风险框架、学习路线、创作内容等，response 覆盖用户约束，并足够具体可执行。

8. 真正无法完成时，限制处理充分。
   如果工具、政策、认证、版权、外部系统能力确实不允许完成，assistant 清楚说明限制原因，并给出具体替代方案、操作步骤、可访问资源、需要补充的信息，或正确升级/转人工。

9. 缺少必要信息时，澄清是合理且充分的。
   如果 email、username、ID、认证码、会议标题、精确位置等是必需且无法从上下文安全推断的，assistant 明确说明需要什么以及拿到后会做什么，可以给 2 分。

10. 当前轮只是确认、收尾或状态回应。
    如果用户只是感谢、表示等待、说不需要继续、提供状态，assistant 合理确认或收尾，不额外制造任务，也可以给 2 分。

## 1 分：部分满足

满足以下任一情况，且没有完全失败到 0 分时，给 1 分：

1. 正确工具调用已经发起，但没有返回用户可见结果。
   assistant 识别了正确工具或流程，但 final response 停在 tool_call / tool_calls，用户还没有看到数据、结论、确认或分析。

2. 多步骤任务只完成了一部分。
   例如只完成前置查询，未做后续详情、验证、比较、发送、保存、确认、分析或用户明确要求的第二/第三步。

3. 漏掉一个或多个重要子需求。
   用户要求 A+B+C，assistant 只做 A/B；或漏掉显式关键词、地区、时间范围、字段、列表、来源、附件、验证步骤、比较对象等。

4. 有结果但证据不足、分析不严谨或结论范围过大。
   例如用很窄的 tool result 推断全局结论，未说明工具无法覆盖 requested scope，缺少必要计算、矩阵、对比、定量指标或验证。

5. 解释了限制，但替代方案不够具体。
   assistant 说明不能访问实时数据、网页、论文、天气、文件、平台或数据库，但只给泛泛建议，没有具体步骤、资源、命令、字段、API、外部网站或可操作路径。

6. 有部分有用内容，但未充分利用可用工具、上下文、默认路径或常识。
   例如有相关工具却没有调用；已有足够上下文却没有推进；可给一般知识帮助却只说不能做。

7. 合理澄清但任务未完成。
   assistant 询问必要信息是合理的，但如果用户原本期待的是实际结果，且当前轮没有结果，通常给 1。若用户问的是“还需要我提供什么”，则可给 2。

8. response 中有局部未验证、占位符、假设或轻中度幻觉。
   如果核心任务仍部分有用，但夹杂未验证 DOI、ASIN、政策、当前时间、名字、报价、链接、字段、placeholder 等，给 1。

9. 格式、语言、单位、输出结构或交付方式部分不符合要求。
   例如没有附上附件、没有按请求显示 raw response、没有分来源呈现、缺少 JSON 字段、没有使用请求语言、单位转换不完整、输出过于冗长或不够精确。

10. 操作可能完成但缺少关键确认。
    例如点击 Buy/Book Now 但没有购买/预订成功确认；注册/更新/发送/提交后没有说明是否成功；声称 email queued 但没有完成用户要求的发送或附件步骤。

11. 最后一轮回答了局部问题，但没有保持当前上下文中的更大目标。
    例如回答了后续选择题，却漏掉用户仍然要求的主实体、最终推荐、原始目标或上下文约束。

## 0 分：未满足

出现以下任一情况，通常给 0 分：

1. 用户没有得到任何可用的核心结果。
   final response 只有 tool call、内部推理、空内容、泛泛说明，或没有返回 requested result / summary / calculation / list / confirmation / explanation。

2. assistant 拒绝、推脱、泛泛道歉或转移话题，且没有有效替代方案。
   尤其是明明可以用可用工具、上下文、常识或简单分析部分满足，却直接说不能做。

3. assistant 要求不必要的澄清。
   用户已经给出足够信息，或上下文/工具/default/合理前置查询可以推进，assistant 却把任务推回给用户，导致核心需求未完成。

4. 工具或参数路径明显错误，导致核心任务无法成立。
   例如调用不匹配工具、用任意编造的 ID、使用错误 retrieval path、只查完全错误对象或错误时间/地点。

5. 核心答案依赖编造或 unsupported critical facts。
   如果主要结论、数据、政策、报价、ID、DOI、链接、统计、工具结果或操作状态无法由上下文/工具支持，并且这会误导用户完成核心任务，应给 0。

6. 只覆盖极小切片且没有交付最终结果。
   例如用户要完整一周/多对象/多步骤结果，assistant 只查一天、一个对象或第一步，并且未返回可用结论。

7. 输出严重违反用户核心格式或合规结构，导致结果不可用。
   例如用户需要保留模板占位符/法律结构/JSON schema/关键字段，但 response 大幅破坏结构，不能作为结果使用。

8. final response 与用户请求无关或回答了错误任务。
   包括把用户的 meta-question 当成执行任务、把选择/确认问题当成实际操作、或完全偏离当前最新用户需求。

# 容易混淆的边界规则

1. tool_call only
   - 用户需要看到结果、确认、分析或最终答案，但 assistant 只发起 tool call：1 分。
   - 当前轮需求本身就是执行/发起该 tool call，且参数正确、无需立即展示结果：2 分。
   - tool call 错误、参数编造、对象错误或方向无效：0 分。

2. 限制、拒绝或无法完成
   - 限制是假的，明明能做却拒绝：0 分。
   - 限制是真的，但替代方案泛泛、不可操作：1 分。
   - 限制是真的，解释准确，并提供具体可行替代、下一步或正确升级：2 分。

3. 幻觉或 unsupported facts
   - 核心答案靠幻觉支撑，用户会被误导：0 分。
   - 大体部分完成，但有非核心或局部未验证内容：1 分。
   - 明确说明不确定性、不编造，并给出可验证下一步：可给 2 分。

4. 多步骤或多子任务
   - 完全没有进入正确链路：0 分。
   - 做了部分关键步骤或部分子任务：1 分。
   - 所有核心步骤完成并汇总：2 分。

5. 格式、语言、结构要求
   - 格式是核心交付物，完全不符合导致不可用：0 分。
   - 内容有用但漏了部分格式/语言/单位/字段：1 分。
   - 内容和格式都满足：2 分。

6. 缺少信息与澄清
   - 必要信息无法推断，assistant 清楚说明需要什么：通常 1 或 2；如果用户当前就是问需要什么，给 2。
   - 信息其实可由上下文、已有工具结果、用户自然语言对象、合理前置查询或明确默认路径获得，却要求用户补充：通常 0 或 1。
   - schema 默认值、示例值、占位符不能自动视为用户真实对象；但如果任务本身允许默认值或用户接受默认值，可不扣分。

7. 最新轮与历史上下文
   - 只评价当前最后一轮 response 对当前有效需求的满足度。
   - 如果用户当前只问一个局部确认，不要求完成整个历史任务，局部确认满足即可给 2。
   - 如果历史目标仍被当前轮继续要求，不能忽略未完成部分。

# 输出要求

只输出一个严格 JSON object。不要输出 Markdown、推理过程、分析过程或 JSON 之外的任何文本。

JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

字段填写要求：

- `score`: 只能是 0、1、2。
- `reason`: 简要说明为什么给这个分数，必须直接指出核心满足/未满足边界。
- `unmet_requirements`: 列出未被满足或只被部分满足的用户需求；如果没有，返回空数组。
- `requirement_analysis`: 简要列出识别到的当前有效主要用户需求。可以用一句话或短列表式文本，但必须是 string。

# 输入数据

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
