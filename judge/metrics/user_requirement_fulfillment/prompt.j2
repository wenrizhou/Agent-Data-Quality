以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估 assistant 最后一轮 response 的“需求满足度（User Requirement Fulfillment）”。

定义：
该指标用于评价 assistant 最后一轮 response 是否已经充分满足 user message 和 conversation context 中提出的所有需求。这里的“需求”包括主任务、子问题、格式要求、内容要求、语言要求、约束条件、工具使用要求，以及多轮上下文中仍然有效的用户目标。

请重点检查：
1. assistant 是否识别并回应了 user 的主任务；
2. 是否遗漏了 user 明确提出的子问题或重点关注点；
3. 是否遵守了 user 对输出格式、语言、长度、结构、内容范围的要求；
4. 如果用户要求执行操作，final response 是否说明操作结果，且该结果足以回应用户目标；
5. 如果任务无法完成，assistant 是否清楚说明无法完成的原因、已完成部分和后续可行步骤；
6. 是否存在只回答部分需求、转移话题、泛泛而谈、过度简化、没有给出用户真正需要的最终结果等问题；
7. 对用户核心目标的满足情况应比边缘性格式或措辞更重要。

评分方法：
请根据 final response 对用户全部需求的满足程度，对该指标打 0、1、2 分。评分时不需要机械统计需求数量，而应综合考虑遗漏需求的数量、重要性、是否影响核心任务、是否违反用户明确约束，以及 final response 是否足以让用户认为任务已经完成。

- 2 分：充分满足。
  assistant 最后一轮 response 已回应 user message 和仍然有效的 conversation context 中的核心需求；主任务、关键子问题、重要约束、格式/语言/结构/内容范围要求均得到满足。若存在轻微措辞或边缘格式问题，但不影响用户认为任务完成，仍可判 2。
- 1 分：部分满足。
  assistant 回应了主要方向或完成了部分核心任务，但遗漏了部分子问题、重点关注点、明确约束、格式/语言/长度/结构要求，或对操作结果说明不够完整。整体有帮助，但用户仍需要补充追问或额外工作才能完成目标。
- 0 分：未满足。
  assistant 没有回应用户主任务，明显转移话题，遗漏核心需求，输出与明确要求严重不符，未给出用户真正需要的最终结果，或任务无法完成时没有清楚说明原因、已完成部分和后续步骤，导致用户目标基本无法达成。

注意事项：
1. 只评价最后一轮面向用户的 final response 是否满足需求；不要单独评价中间工具调用质量，除非 final response 未能基于执行结果回应用户目标。
2. 如果用户要求执行操作，final response 不一定必须很长，但应清楚说明操作结果、失败原因或剩余步骤，使用户知道目标是否达成。
3. 如果任务因权限、工具失败、外部系统限制、缺少信息而无法完成，assistant 清楚说明限制、已完成部分和后续可行步骤时，可根据满足程度判 1；如果它含糊带过或让用户误以为已完成，通常判 0。
4. 如果 user message 有明确输出格式、语言、长度、结构或内容范围要求，应把这些作为需求的一部分；但评分时核心任务满足情况优先于边缘措辞。
5. 不要因为 final response 没有提供用户没有要求的信息而扣分。
6. 如果最后一轮 assistant message 只是工具调用而没有面向用户的 final response，应按需求未完整满足处理，并在 reason 中说明。
7. 多轮上下文中已经被用户取消、替换或不再相关的旧需求，不应作为当前未满足需求扣分；仍然有效的约束和目标应纳入评价。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

字段说明：
- score: 0 / 1 / 2
- reason: 简要说明为什么给这个分数
- unmet_requirements: 如果存在，请列出未被满足或只被部分满足的用户需求
- requirement_analysis: 简要列出你识别到的主要用户需求

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}

raw_record:
{{ raw_json }}
