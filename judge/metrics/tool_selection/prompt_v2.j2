以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估工具调用轨迹中的“工具选择”质量。
“工具选择”指的是：assistant 是否为用户 query 和 conversation 中的任务选择了合适的工具/API，以及是否漏用了必要工具、调用了无关工具，或以错误顺序组织工具链。

请只评估“选择哪些工具/API”这一件事，不评价同一次工具调用内部字段填写细节。

请判断工具选择中是否存在以下问题：

1. 必要工具缺失
指用户任务需要某类工具/API 才能完成，当前可用工具集中存在对应工具或具备等价能力的替代工具，但 assistant 没有调用。

注意：如果当前可用工具集中没有能完成用户核心需求的工具，assistant 正确识别能力不足、没有调用无关工具、不虚构工具能力，则不属于必要工具缺失，应判 2 分。

2. 工具能力不匹配
指 assistant 调用了与用户核心任务明显不匹配的工具/API，例如用天气工具查股票、用音乐榜单工具找餐厅、用地点搜索工具查询用户资料，或用错误领域的 API 处理用户请求。

注意：本指标不要求 judge 判断某个相关工具的返回字段是否足够覆盖用户需求，也不评价工具实际返回内容是否完整。只要工具的 name、description 或 schema 显示其能力方向与用户核心任务匹配，就不要因为返回字段覆盖不足、结果不完整、时效性不够细、没有专门子字段而扣 tool_selection 分。这些属于工具能力限制、结果充分性或后续回答质量问题，不属于本指标。

3. 无关工具调用
指 assistant 调用了与用户任务、conversation 上下文、前序 tool response 都没有合理关系的工具/API。

4. 工具链顺序错误
指多步任务中工具调用顺序不合理，例如在需要先搜索得到 ID/URL/候选项后，直接调用详情工具；或在需要先读取文件后，直接发送/总结文件内容。

评分标准：

- 2 分：工具选择正确。
  assistant 调用了完成用户任务所需的工具或等价能力工具；没有明显漏掉当前可用的核心工具；没有明显无关工具；多步工具链顺序合理。
  如果可用工具集中没有能完成用户核心需求的工具，assistant 正确识别能力不足、没有调用无关工具、不虚构工具能力，应判 2 分，而不是 0 分。
- 1 分：轻微工具选择问题。
  存在轻微冗余工具、少量无关但不影响核心任务的工具、次优但仍可完成任务的工具，或漏掉非核心辅助工具；整体任务仍有合理完成路径。
  如果当前轮没有补全后续工具，但已经调用了与当前步骤匹配的前置工具，且后续工具链可以在下一轮继续完成，应判 1 分，而不是 0 分。
- 0 分：工具选择错误。
  存在当前可用的核心工具缺失、明显无关工具完成核心任务，或工具链顺序严重错误，导致用户任务无法正确完成或明显偏离用户需求。
  只有当可用工具集中存在能完成核心需求的工具或等价能力工具，而 assistant 没有调用，才判必要工具缺失。

注意事项：
1. 不要因为工具调用内部字段填写细节直接扣分。如果工具类型和能力本身是合适的，本指标通常仍可判为工具选择正确。
2. 如果工具调用失败，不要仅因为失败就判工具选择错误；只有失败反映出缺少前置工具或工具链顺序错误时，才属于本指标问题。
3. 如果 assistant 使用的工具名称与用户表达不完全一致，但工具描述显示它具备完成任务的能力，应判为工具选择正确。
4. 如果有多个可替代工具，只要选择的工具具备等价能力，不应因没有选择“最佳”工具而扣分。
5. 如果用户任务本身不需要工具，assistant 没有调用工具不应判为必要工具缺失。
6. 如果用户任务需要工具，但当前可用工具集中没有能完成用户核心需求的工具，assistant 正确说明能力不足、没有调用无关工具、没有虚构工具能力，应判 2 分。
7. 如果当前可用工具集中存在明确能完成用户核心需求的工具，而 assistant 完全没有调用，且没有可执行的替代方案，应判为必要工具缺失。
8. 如果一个工具调用看起来无关，但它是为了获取后续工具所需的信息、验证前置条件、查找候选项或读取上下文，可以视为合理工具链的一部分。
9. 如果错误只出现在非核心辅助工具选择上，且核心任务仍能完成，可以考虑判为 1。
10. 如果 assistant 已经调用了当前步骤所需的前置工具，但没有在当前轮补全全部后续工具链，且未错误使用无关工具，应判为 1。
11. 不要评价“工具能力是否完全覆盖核心需求”的细节。本指标只判断 assistant 是否在可用工具中选择了方向匹配的工具，不判断该工具返回字段是否足够细、结果是否完整、是否能完全覆盖用户最终需求。
12. 如果核心工具明显选错，例如领域完全不相关或能力完全无关，即使该调用可以执行，也应判为 0。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
