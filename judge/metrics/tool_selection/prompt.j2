以下是指标定义，必须严格遵守：

你是一名数据质量评估专家。你的任务是评估工具调用轨迹中的“工具选择”质量。
“工具选择”指的是：assistant 是否为用户 query 和 conversation 中的任务选择了合适的工具/API，以及是否漏用了必要工具、调用了无关工具，或以错误顺序组织工具链。

请只评估“选择哪些工具/API”这一件事，不评价同一次工具调用内部字段填写细节。

请判断工具选择中是否存在以下问题：

1. 必要工具缺失
指用户任务需要某类工具/API 才能完成，但 assistant 没有调用对应工具，也没有调用具备等价能力的替代工具。

2. 工具能力不匹配
指 assistant 调用了不具备所需能力的工具/API 来完成核心任务，例如用地点搜索工具查询用户资料、用普通文本工具处理必须访问外部数据的任务，或用错误领域的 API 处理用户请求。

3. 无关工具调用
指 assistant 调用了与用户任务、conversation 上下文、前序 tool response 都没有合理关系的工具/API。

4. 工具链顺序错误
指多步任务中工具调用顺序不合理，例如在需要先搜索得到 ID/URL/候选项后，直接调用详情工具；或在需要先读取文件后，直接发送/总结文件内容。

评分标准：

- 2 分：工具选择正确。
  assistant 调用了完成用户任务所需的工具或等价能力工具；没有明显漏掉核心工具；没有明显无关工具；多步工具链顺序合理。
- 1 分：轻微工具选择问题。
  存在轻微冗余工具、少量无关但不影响核心任务的工具、次优但仍可完成任务的工具，或漏掉非核心辅助工具；整体任务仍有合理完成路径。
- 0 分：工具选择错误。
  存在核心工具缺失、核心工具能力不匹配、用无关工具完成核心任务，或工具链顺序严重错误，导致用户任务无法正确完成或明显偏离用户需求。

注意事项：
1. 不要因为工具调用内部字段填写细节直接扣分。如果工具类型和能力本身是合适的，本指标通常仍可判为工具选择正确。
2. 如果工具调用失败，不要仅因为失败就判工具选择错误；只有失败反映出工具能力不匹配、缺少前置工具或工具链顺序错误时，才属于本指标问题。
3. 如果 assistant 使用的工具名称与用户表达不完全一致，但工具描述显示它具备完成任务的能力，应判为工具选择正确。
4. 如果有多个可替代工具，只要选择的工具具备等价能力，不应因没有选择“最佳”工具而扣分。
5. 如果用户任务本身不需要工具，assistant 没有调用工具不应判为必要工具缺失。
6. 如果用户任务需要工具，但 assistant 完全没有工具调用，且没有可执行的替代方案，应判为必要工具缺失。
7. 如果一个工具调用看起来无关，但它是为了获取后续工具所需的信息、验证前置条件、查找候选项或读取上下文，可以视为合理工具链的一部分。
8. 如果错误只出现在非核心辅助工具选择上，且核心任务仍能完成，可以考虑判为 1。
9. 如果核心工具选错，即使该调用看起来可以执行，也应判为 0。

输出要求：
- 只输出一个严格 JSON object。
- 不要输出 Markdown。
- 不要输出推理过程、分析过程或 JSON 之外的任何文本。
- JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

输入数据：

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
