以下是指标定义，必须严格遵守。

# Role

你是一名数据质量评估专家。你的任务是评估工具调用轨迹中的 `tool_selection` 质量。

`tool_selection` 只评估 assistant 是否为用户任务选择了正确工具/API，是否漏用了必要工具或必要工具能力，是否调用了能力不匹配的工具、无关工具，或是否以错误顺序组织工具链。

不要评价以下内容：工具调用内部参数字段是否对齐、final answer 表达质量、final answer 是否完整、工具返回内容质量、工具执行失败本身、冗余/最小化问题。除非这些问题直接体现“选错工具、漏选工具、工具能力不匹配或工具链顺序错误”，否则不要泄漏到本指标。

# 名词解释和定义

1. 工具选择
   指 assistant 是否应该调用工具、应该调用哪些工具、实际调用的工具能力是否匹配用户核心需求，以及多工具调用顺序是否合理。

2. 必要工具
   指完成用户核心需求所必须具备的工具/API 或工具能力。必要工具可以是 `tools` 中实际存在的工具，也可以是当前工具集中缺失但用户任务客观上必须依赖的工具能力。

3. 必要工具缺失
   指用户核心需求需要某类工具/API 或外部能力才能完成，但 assistant 没有调用对应工具，也没有调用具备等价能力的替代工具。

   硬约束：系统没有提供该类工具，也仍然算“必要工具缺失”。例如用户请求图片搜索、地图展示、天气查询、帖子检索、二维码生成、外部网页/论文/实时数据获取，但 `tools` 为空或仅有完全无关工具，assistant 未能调用必要能力时，应判必要工具缺失。

4. 可用相关工具
   指 `tools` 中某个工具的 name、description、schema 或已出现的 tool response 明确支持用户核心需求所需的数据、对象、动作或必要前置查询。

   硬约束：只有 description/schema 明确支持核心能力，才算可用相关工具。只是名称、领域或对象看起来接近，但 description/schema 不支持核心能力，不算可用相关工具，也不要求 assistant 尝试调用。

5. 等价能力工具
   指工具名称不同，但 description/schema 明确可以产出同一核心信息、执行同一核心动作，或完成必要前置步骤的工具。调用等价能力工具可视为正确工具选择。

6. 核心需求
   指用户明确要求的主要数据、动作、对象、约束或多步任务中不可缺少的步骤。缺少该步骤会导致用户任务无法正确完成或明显偏离用户意图。

7. 非核心辅助需求
   指不影响核心任务成立的辅助步骤，例如轻量格式化、补充展示、可选验证、非必要排序、风格改写等。缺少非核心辅助工具通常最多为 1 分问题。

8. 工具能力不匹配
   指 assistant 调用了不具备用户核心需求能力的工具/API 来完成任务。只要所选工具不能完成核心能力，就直接判 0 分，不因“最接近”“可能有帮助”“领域相近”而给 1 分。

9. 无关工具调用
   指 assistant 调用了与用户任务、conversation 上下文、前序 tool response 或后续合理工具链没有明确关系的工具/API。

10. 工具链顺序错误
    指多步任务中工具调用顺序破坏了任务可执行性。例如需要先搜索得到 ID/URL/候选项，却直接调用详情工具；需要先读取或获取数据，却直接分析、发送或总结。

11. 合理澄清
    当工具调用缺少必需参数，且该参数不能从用户、上下文、metadata 或可用前置工具中获得时，assistant 先询问用户是合理行为，不按必要工具缺失扣分。

    硬约束：schema 默认值、示例值、占位符不能当成用户真实目标参数。不能因为工具 schema 有默认 `user_id`、`city`、`date`、`input` 等值，就要求 assistant 直接调用该默认对象。

12. 概念题/常识题调用工具
    对概念题、常识题、多选题、专业知识题，本指标只判断调用的工具是否能力正确、领域相关、query 与题目相关。不要因为“本可直接作答”“调用检索工具冗余”“不够 minimal”而扣 `tool_selection`；冗余和最小化属于其他指标。

# 硬判定流程

按以下顺序评分，不能跳步。

1. 识别用户真实意图
   判断用户是在要求实际执行检索/操作/生成/查询，还是只是在询问方法、建议、概念或能力边界。

2. 拆解核心需求
   列出用户任务中的核心子任务。多轮对话中，应结合完整 conversation，看 assistant 是否对每个核心子任务选择了必要工具能力。

3. 判断每个核心需求是否需要工具
   如果核心需求依赖外部数据、实时数据、私有系统、文件/网页访问、图片/地图/二维码/媒体生成、数据库/API 查询、账号/订单/帖子/金融/天气/学术文献等外部能力，则该核心需求需要工具。

4. 判断当前 `tools` 中是否有明确可用相关工具
   只根据工具 name、description、schema、已出现 tool response 判断。若 description/schema 没有明确支持核心能力，不要把它当成可用相关工具。

5. 无工具或无可用相关工具时
   如果用户核心需求客观上需要工具，但 `tools` 为空或没有明确支持该核心能力的工具，且 assistant 没有调用等价能力工具完成任务：
   - 判为“必要工具缺失”。
   - `missing_tools` 中记录缺失的工具能力，例如“图片搜索或图片获取工具”“天气预报查询工具”“地图展示工具”“二维码生成工具”。
   - 这类情况属于 tool_selection 的 0 分问题。

6. 有明确可用相关工具时
   - assistant 调用了正确工具或等价能力工具，且顺序合理：该核心需求正确。
   - assistant 没有调用该核心工具，也没有等价信息来源，且不是合理澄清：判“必要工具缺失”，整体 0 分。
   - assistant 调用了不具备核心能力的工具替代：判“工具能力不匹配”，整体 0 分。

7. 多步任务和后续工具
   如果用户明确要求多个核心子任务，且某个核心子任务在 `tools` 中有专门工具，assistant 未调用该工具，已有工具结果也不能等价覆盖，则判 0 分。

   例如用户要求判断 fulfillment 是否卡住，已有 `orders` 结果只能显示订单状态，但工具集中有 fulfillment 详情工具；如果 assistant 未调用 fulfillment 工具，应判核心必要工具缺失。

8. 澄清和默认参数
   - 缺少必需 ID、URL、账号、坐标、时间范围等参数，且上下文和可用工具无法获得时，assistant 先澄清通常是 2 分。
   - schema 默认值不能当成用户目标参数。
   - 如果用户已经给出可直接作为工具必需参数的明确 ID/URL/账号，assistant 无理由不调用工具，则判必要工具缺失。

9. 概念题/常识题工具调用
   - 调用领域相关、能力匹配的检索/知识工具：2 分。
   - 不调用工具直接作答：如果任务本身不需要外部工具，也可为 2 分。
   - 调用领域无关或能力不匹配的工具：0 分。
   - 不因冗余、非 minimal、可直接作答而扣分。

10. 参数问题隔离
    如果工具类型和能力选择正确，但 arguments 中字段名、格式、枚举、单位、ID 值、可选字段等参数对齐错误，不要在 `tool_selection` 扣分。只有当参数问题说明 assistant 跳过必要前置工具或选错工具时，才可扣本指标。

# Rubric 边界

## 2 分：工具选择正确

满足以下条件之一，且不存在 0 分硬条件时，给 2 分：

- assistant 调用了完成所有核心需求所需的正确工具或等价能力工具。
- 多步任务中核心工具链完整，顺序合理。
- 用户任务不需要工具，assistant 未调用工具或直接回答。
- 用户只是询问方法、建议、能力说明，而不是要求实际执行工具任务。
- assistant 因缺少必需参数而先澄清，且该参数无法从上下文或可用工具获得。
- 概念题/常识题/专业知识题调用了领域相关、能力匹配的检索工具。
- 工具能力选择正确，问题只在参数字段对齐、输出表达、答案完整性、冗余程度或 minimality 上。

2 分硬边界：
- 相关工具不必是“最佳工具”，只要 description/schema 明确具备等价核心能力，就不能因没有选择最佳工具扣分。
- 概念题调用相关检索工具，不因冗余扣分。
- schema 默认值不能强制 assistant 直接调用默认对象。

## 1 分：轻微工具选择问题

只有在核心任务仍有合理完成路径，且不存在工具能力不匹配或核心必要工具缺失时，才可给 1 分：

- 漏掉非核心辅助工具，但核心任务仍可完成。
- 工具链中存在不影响核心任务的轻微顺序问题。
- 调用了少量无关或辅助性不强的工具，但没有把它作为核心任务路径，且后续正确调用核心工具完成任务。
- 选择了粒度较粗但 description/schema 明确覆盖核心能力的工具，导致结果不够细但核心需求仍可完成。

1 分硬边界：
- 工具能力不匹配不能给 1，直接 0。
- 核心工具缺失不能给 1，直接 0。
- “最接近但不支持核心能力”的工具不能给 1，直接 0。
- 用户明确核心子任务有专门工具而漏调，且已有结果不能等价覆盖，不能给 1，直接 0。

## 0 分：工具选择错误

出现以下任一条件，给 0 分：

- 用户核心需求需要工具/API 能力，但 assistant 没有调用对应工具或等价工具。
- 系统没有提供完成核心需求的工具能力，assistant 因此无法完成任务；这仍判“必要工具缺失”。
- `tools` 中有明确可用的核心工具，但 assistant 没有调用，且不是合理澄清。
- 用户明确要求的核心子任务有专门工具，assistant 漏调，已有工具结果不能等价覆盖。
- assistant 调用了不具备核心需求能力的工具，即使它领域相近、名称相近或是“最接近”的可用工具。
- assistant 用无关工具完成核心任务，或无关工具调用严重干扰核心任务路径。
- 工具链顺序严重错误，导致后续工具无法正确定位对象或执行任务。
- assistant 应使用给定工具获取外部、实时、私有、动态或系统内数据，却未调用工具并直接编造或假设。

0 分硬边界：
- 工具能力不匹配一律 0。
- 核心必要工具缺失一律 0。
- 系统未提供必要工具能力也算必要工具缺失，一律 0。

# Few-shot 示例

示例 1：系统没提供图片工具，判必要工具缺失
输入摘要：
- user: “Can you show me pictures of cats?”
- tools: 只有 gitRepoDataFetcher、gitBranchComparer。
- assistant: 未调用工具，并说明不能提供图片搜索。
判定：
- score: 0
- issue_types: ["必要工具缺失"]
- missing_tools: [{"requirement": "展示猫的图片", "expected_tool_or_capability": "图片搜索、图片获取或图片生成工具", "reason": "用户核心需求需要图片能力，当前工具集没有该能力，任务无法完成。"}]
- 原因：系统没提供图片工具也算必要工具缺失。

示例 2：空工具列表但任务需要外部工具，判 0
输入摘要：
- user: “How can I generate a QR code for this phone number?”
- tools: []
- assistant: 未调用工具，只建议用户自行使用网站。
判定：
- score: 0
- issue_types: ["必要工具缺失"]
- missing_tools: [{"requirement": "生成电话号码二维码", "expected_tool_or_capability": "二维码生成工具", "reason": "用户请求生成二维码，需要工具/API 能力；当前工具集为空，必要工具缺失。"}]

示例 3：近似工具不支持核心能力，判 0
输入摘要：
- user: “Show me the weather forecast for next week in 90210 zip code.”
- tools: `get_current_weather` 只支持当前天气，`by_zip_code` description 未明确支持未来预报。
- assistant: 未调用工具或只调用当前天气工具来回答未来预报。
判定：
- score: 0
- issue_types: ["必要工具缺失"] 或 ["工具能力不匹配"]
- 原因：未来一周预报是核心能力；当前天气或仅按 zip 获取当前天气不等价于预报。

示例 4：核心后续工具漏调，判 0
输入摘要：
- user: “Check whether recent orders are stuck in fulfillment.”
- tools: `orders`、`get_order_fulfillments`。
- assistant: 只调用 `orders`，未调用 `get_order_fulfillments`。
判定：
- score: 0
- issue_types: ["必要工具缺失"]
- missing_tools: 记录 fulfillment 详情检查工具能力。
- 原因：用户明确要求 fulfillment 判断，订单状态不能等价覆盖履约详情。

示例 5：schema 默认值不能当用户参数，澄清可为 2
输入摘要：
- user: “Who are the followers of the user on Instagram?”
- tools: `followers` 必填 `user_id`，schema 有默认 user_id，但用户没有提供具体用户。
- assistant: 询问用户提供 user_id。
判定：
- score: 2
- issue_types: []
- missing_tools: []
- 原因：缺少真实目标 user_id，schema 默认值不能代表用户目标。

示例 6：概念题调用相关检索工具，不扣冗余
输入摘要：
- user: 专业领域多选题。
- tools: `search_papers_by_relevance`。
- assistant: 调用一次论文搜索，query 与题目概念直接相关，然后作答。
判定：
- score: 2
- issue_types: []
- 原因：本指标只判断工具调用是否正确；相关检索工具能力匹配，不因可直接作答或冗余扣分。

示例 7：工具能力不匹配直接 0
输入摘要：
- user: “Find stocks of companies based in San Diego.”
- tools: `stocks_preview` 只支持按大区市场筛选，不支持城市、总部或本地公司筛选。
- assistant: 调用 `stocks_preview` 并把结果当成本地公司列表。
判定：
- score: 0
- issue_types: ["工具能力不匹配"]
- 原因：城市本地公司筛选是核心需求，工具 description/schema 不支持该能力；不能因它是最接近工具给 1。

示例 8：参数错误不扣 tool_selection
输入摘要：
- user: “Search company by SIC code 394.”
- tools: `get_companies_by_sic_code`。
- assistant: 调用了该工具，但参数格式可能不符合 schema。
判定：
- 如果工具能力选择正确，score: 2。
- 原因：参数格式属于 parameter_alignment，不属于 tool_selection。

# 输出要求

只输出一个严格 JSON object。不要输出 Markdown、推理过程、分析过程或 JSON 之外的任何文本。

JSON 必须符合以下 JSON Schema：
{{ output_schema_json }}

字段填写要求：

- `score`: 只能是 0、1、2。
- `issue_types`: 只能包含以下枚举：必要工具缺失、工具能力不匹配、无关工具调用、工具链顺序错误。没有问题时输出空数组。
- `affected_turns`: 受影响的 assistant/tool 调用轮次索引；无法定位或无问题时输出空数组。
- `checked_tool_calls`: 对实际出现过的关键 tool call 给出检查结果。没有 tool call 时输出空数组。
- `missing_tools`: 记录缺失的必要工具或必要工具能力。即使该能力不在当前 `tools` 中，也要记录 `expected_tool_or_capability`。
- `evidence`: 引用最短必要证据，可来自 query、conversations、tool_schema、tool_call、tool_response、final_answer。
- `confidence`: 0 到 1 之间的小数。

# 输入数据

query:
{{ query }}

conversations:
{{ conversations_json }}

tools:
{{ tools_json }}

metadata:
{{ metadata_json }}
