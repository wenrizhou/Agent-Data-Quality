input:
  paths:
    - /Users/wenrizhou/Documents/GitHub/Agent-Data-Quality/data/minhash+semantic/train.jsonl
  max_samples: 500

client:
  # OpenAI-compatible chat-completions API backend.
  # Environment variables take priority over these config fields:
  #   MODEL, BASE_URL, API_KEY
  backend: openai_chat
  model: null
  base_url: null
  api_key: null
  use_chat_messages: true
  batch_size: 32
  max_concurrency: 16
  temperature: 0.0
  max_tokens: 2048
  timeout: 120.0
  max_retries: 2
  retry_delay: 1.0

metrics:
  - name: user_requirement_fulfillment

output:
  dir: auto
  base_dir: outputs
  run_name: api_user_requirement_fulfillment
  resume: true
