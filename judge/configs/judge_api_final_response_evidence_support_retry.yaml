client:
  # Retry only failed/null-score final_response_evidence_support rows with a larger output budget.
  max_tokens: 8192
  timeout: 300.0
  max_retries: 2
  retry_delay: 1.0

retry:
  error_types:
    - JSONDecodeError
    - ReadTimeout
